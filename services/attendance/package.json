{
    "name": "@jouleops/attendance",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "main": "src/index.ts",
    "scripts": {
        "dev": "bun --hot src/index.ts",
        "start": "bun src/index.ts",
        "build": "bun build ./src/index.ts --outdir ./dist --target bun",
        "test": "bun test src/geofenceUtils.test.ts"
    },
    "dependencies": {
        "@jouleops/shared": "workspace:*"
    },
    "devDependencies": {
        "@types/bun": "latest"
    }
}